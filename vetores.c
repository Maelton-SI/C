#include <stdio.h>

int main(int argc, char const *argv[])
{
    //vetores sao obrigatoriamente sequencias (em relação à ocupação de espaços na memória)
    //uma lista em python, por exemplo, não é necessariamente sequencial
    //os dados podem estar em qualquer local da memória e o python fornece para a gente essa abstração

    //vetores sao estruturas mais arcaicas e simples
    //preciso especificar o tipo do conteúdo e o tamanho do vetor no momento de sua inicialização

    //não consigo imprimir o vetor inteiro como na linguagem python
    //preciso obrigatoriamente indexar todas as opções que desejo imprimir

    //nao existe o tipo String em C, se eu quiser criar uma string devo criar um vetor de caracteres
    
}